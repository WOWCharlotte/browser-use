# AG-UI 协议集成 - 思维导图

```mermaid
tree
root[AG-UI 协议集成]
  core[核心目标]
    goal1[前后端通信标准化]
      current[当前问题]
        custom_sse[自定义 SSE 格式]
        no_agui_events[未实现 AG-UI 事件]
      target[目标]
        agui_protocol[AG-UI 协议事件]
        copilotkit[CopilotKit 集成]
    goal2[前端 Agent 组件化]
      components[组件]
        chatwindow[ChatWindow 重构]
        provider[CopilotKitProvider]
        agent[useAgent Hook]
    goal3[后端 HTTP Agent 端点]
      endpoint[AG-UI 端点]
        run_agent_input[RunAgentInput 解析]
        event_stream[BaseEvent SSE 流]
  modules[涉及模块]
    frontend[前端: frontend/src/]
      api[API 层]
        copilotkit_route[CopilotKit API Route]
      components[组件层]
        chat[ChatWindow]
        provider[Agent Provider]
    backend[后端: backend/app/]
      api[API 层]
        chat_endpoint[chat.py 重构]
      service[服务层]
        agent_service[AgentService 改造]
  events[AG-UI 事件类型]
    lifecycle[生命周期]
      RUN_STARTED[RunStartedEvent]
      RUN_FINISHED[RunFinishedEvent]
      RUN_ERROR[RunErrorEvent]
    message[消息事件]
      TEXT_START[TextMessageStartEvent]
      TEXT_CONTENT[TextMessageContentEvent]
      TEXT_END[TextMessageEndEvent]
    tool[工具事件]
      TOOL_START[ToolCallStartEvent]
      TOOL_ARGS[ToolCallArgsEvent]
      TOOL_RESULT[ToolCallResultEvent]
      TOOL_END[ToolCallEndEvent]
    state[状态事件]
      STATE_SNAPSHOT[StateSnapshotEvent]
      STATE_DELTA[StateDeltaEvent]
    step[步骤事件]
      STEP_START[StepStartedEvent]
      STEP_END[StepFinishedEvent]
  tasks[任务拆分]
    task1[后端 AG-UI 端点]
    task2[前端 API Route]
    task3[前端 CopilotKit 集成]
    task4[ChatWindow 重构]
    task5[单元测试]
  risks[风险]
    backward_compat[向后兼容性]
      existing_sessions[现有会话处理]
    complexity[迁移复杂度]
      large_refactor[大规模重构]
